{% extends 'base.html.twig' %}

{% block title %}Agences{% endblock %}

{% block body %}
    <h1>Agences</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ agence.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ agence.nom }}</td>
            </tr>
            <tr>
                <th>Ville</th>
                <td>{{ agence.ville }}</td>
            </tr>
            <tr>
                <th>Departement</th>
                <td>{{ agence.departement }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_agences_index') }}">back to list</a>

    <a href="{{ path('app_agences_edit', {'id': agence.id}) }}">edit</a>

    {{ include('agences/_delete_form.html.twig') }}
{% endblock %}
